import React, { Component } from 'react';
import Protected from './Protected';

class App extends Component {
  render() {
    return (
      <div>
        <Protected />
      </div>
    );
  }
}

export default App;
